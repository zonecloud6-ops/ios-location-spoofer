name: Sync upstream mirror

on:
  workflow_dispatch:
  schedule:
    - cron: '17 2 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check Actions runner
        run: echo "Actions runner is available"
